# Backend Build Log

A running diary of the backend implementation, in the order things were actually built — not a retroactive summary. Append a new entry every time a meaningful piece is completed, not just once at the end.

Format per entry:

```
## YYYY-MM-DD HH:MM — <short title>
**Master prompt section:** e.g. "Section 6 — POST /profiles/{id}/drift"
**What was implemented:** plain-language description
**Decisions made where the prompt was ambiguous:** state the ambiguity and what you chose, and why
**Blockers hit / still open:** anything unresolved, anything that needs the human's input
```

---

<!-- Entries start below this line. Do not remove this comment — it marks where the agent should begin appending. -->

## 2026-07-22 — Kickoff: discrepancy with prior memory, plan set
**Master prompt section:** Section 0 — ground rules
**What was implemented:** Nothing yet. Verified current repo state before starting: confirmed there is no `backend/` directory and no `be/main` branch anywhere in this repo's git history (local or remote) despite an earlier session's memory claiming a complete backend existed at commit `80700cc` — that work, if it exists, lives outside this working directory and was not recoverable from here. User confirmed: proceed by building the backend fresh against this master prompt, on top of the actual `extension/src/` frontend code as the authoritative contract.
**Decisions made where the prompt was ambiguous:** The master prompt's Section 3 `Constraints` shape (`rating?: number`, `occasion?: string[]`) does not exactly match the real frontend type in `extension/src/api/types.ts` (`rating?: { min: number }`, `occasion?: string`, singular). Per Section 0.1, the actual frontend code wins — the drift engine and all schemas will use the real shape: `rating: { min: number }` (numeric distance on `.min`) and `occasion: string` (exact-match distance, same treatment as `category.gender`), not a Jaccard set.
Also: the master prompt assumes the frontend "already has a configurable backend-URL field defaulting to `http://localhost:8000`" — no such settings field exists anywhere in `extension/src`. Decision: hardcode `http://localhost:8000` as a constant default in the new `src/api/client.ts` rather than building a settings UI (out of scope per Section 11's "do not add features beyond what's asked").
**Blockers hit / still open:** None yet. Docker and Docker Compose are available locally; using Postgres via docker-compose as primary per Section 2, mapped to host port 5433 (native Windows Postgres already owns 5432 on this machine).

## 2026-07-22 — Backend implemented and smoke-tested end to end
**Master prompt section:** Sections 1, 2, 4, 5, 6, 7, 8 (nearly the whole backend)
**What was implemented:** Full FastAPI backend under `backend/app/`: SQLAlchemy models for `accounts`/`personas`/`profiles`/`events` (`models.py`), the event fold/projection logic (`projection.py`), the deterministic drift engine (`drift.py`), the Groq key-rotation client with strict/non-strict fallback (`ai/groq_client.py`), the two AI touchpoints (`ai/suggest_name.py`, `ai/drift_phrasing.py`), and all six endpoint groups (`routers/personas.py`, `routers/profiles.py`, `routers/ai.py`) wired together in `main.py` with CORS. Ran `docker compose up -d db` (Postgres 16-alpine on host port 5433) and `uvicorn app.main:app --port 8000 --reload`, then smoke-tested every endpoint with curl: persona create/list, profile create/list/get, drift (both a small in-place change and a big category change, confirming `update`/`new_profile` decisions), commit in all three modes, rollback, delete (soft), and `/ai/suggest-name`. The Groq calls succeeded on the first real key in rotation (key #2) using `strict: true` structured output with `openai/gpt-oss-20b` — both the AI-phrased drift reason and the AI-suggested name came back well-formed. CORS preflight against a `chrome-extension://` origin returns the correct `access-control-allow-origin` header.
**Decisions made where the prompt was ambiguous:** Found and fixed a real bug during smoke testing: `_next_version_number` (used for `mode: "new_version"` in the commit endpoint) assumed every `ProfileCreated`/`VersionCommitted` event payload has a `"version"` key, but `ProfileCreated` payloads don't carry one (version 1 is implicit) — this threw `KeyError: 'version'` on the very first `new_version` commit. Fixed by treating `ProfileCreated` as version 1 explicitly when computing the historical max. Verified the fix also confirms the earlier design decision (next version = historical max + 1, not projected-version + 1) actually works: created v1 → committed v2 → rolled back to v1 → committed new_version again → correctly got v3 (not a reused v2), then `update` correctly overwrote v3 in place, then `new_profile` correctly forked a new profile_id at v1.
Used SQLAlchemy's generic `JSON` column type for `events.payload` (not Postgres-specific `JSONB`) so the same model works unchanged if this ever needs to fall back to SQLite — no fallback was needed here since Docker was available, but this keeps the option open per Section 2 without extra code.
**Blockers hit / still open:** None. Backend is running locally at `http://localhost:8000` against Postgres on port 5433. Not yet deployed to AWS — CORS is currently permissive (`allow_origin_regex` for any `chrome-extension://*`), which is correct for local dev per Section 2 but must be tightened to the real extension ID before any public deployment.

## 2026-07-22 — Fixes from real-browser testing: missing `neck` field, incomplete URL round-trip, version labeling
**Master prompt section:** Section 3 (Constraints contract — extended, see decision below), Section 7 (drift weights), Section 5/6 (event payload + response shape for version labels/history)
**What was implemented:** The user actually loaded the unpacked extension in Chrome and tested it against live Myntra (something I hadn't been able to do myself — no browser-automation tool was available in my session). They found three real bugs:
1. **Only 3 of 6+ applied filters were captured.** `src/adapter/urlSchema.ts` only ever parsed `Brand`/`Fabric`/`Sleeve`/`Size`/`Color` out of Myntra's `f=` query param — it silently dropped `Neck` (no corresponding field existed anywhere in `Constraints`) and never parsed `Occasions` at all (despite `Constraints.occasion` already existing in the type). Fixed by: adding a new optional `neck?: { include: string[] }` field to `Constraints` (frontend `types.ts` + backend `schemas.py`, mirroring the existing `sleeve` field exactly), and parsing `Occasions` (Myntra's facet is comma-separated/multi-select, `Constraints.occasion` is a single string — joined multi-selections with `,` into one string rather than dropping all but one).
2. **Reopening a saved profile lost most of the original filters.** `buildUrlFromConstraints` only ever serialized `Brand`/`Fabric`/`Size`/`Color` back into the `f=` param — even fields that WERE captured (`sleeve`) were never written back out. Fixed by adding `Sleeve`, `Neck`, and `Occasions` to the URL builder too, so save → reload → reapply is now lossless for every field the parser captures.
3. **No way to name/label a version**, only ever "v1"/"v2". Extended the existing `commit` endpoint's already-present `name` field (previously only used for `mode=new_profile`) to also act as an optional version label for `new_version`/`update` — stored as `"label"` in the `VersionCommitted` event payload, propagated through `projection.py`'s fold, and surfaced two new additive response fields on `ProfileVersionOut`: `versionLabel` (the current version's label) and `history` (the full list of `{version, label, createdAt}` entries, deduped by version number so an `update`-mode commit correctly replaces its version's entry rather than duplicating it, per the "Update Current: overwrite silently" semantics already in the three-way save modal). No new request fields were needed anywhere.
**Decisions made where the prompt was ambiguous:** Adding `neck.include` as a new drift field would have broken the Section 7.2 invariant that weights sum to exactly 1.0. Rather than rebalancing every other field's weight (which the prompt explicitly says not to casually touch for `category.articleType`), I carved the new field's weight out of the existing `sleeve.include: 0.02` budget by splitting it evenly: `sleeve.include: 0.01` + `neck.include: 0.01`. This is the smallest possible change that preserves the sum-to-1.0 invariant and treats both as the same tier of "minor style attribute" they conceptually are.
**Blockers hit / still open:** Caught a real deployment bug while re-testing this: `uvicorn --reload` silently failed to actually restart the worker process after a file change (`WatchFiles detected changes... Reloading...` printed, but no new `Started server process` line ever followed) — the OLD code kept serving requests for several curl round-trips before I noticed the `neck` field was missing from responses despite being in the code. Killed the whole process tree and restarted without `--reload`; recommend NOT relying on `--reload` for the rest of this build — restart manually after each backend edit and verify via `/health` + a quick response-shape check, not just a 200 status, since a stale reload will still return 200s.

## 2026-07-22 — Second round of real-browser bugs: wrong facet key, and a generic catch-all for the rest
**Master prompt section:** Section 3 (Constraints contract, extended again)
**What was implemented:** More real-browser testing turned up that even the *named* fabric field wasn't working: Myntra's actual facet key is `"Fabric Types"`, not `"Fabric"` — the parser's `startsWith("Fabric:")` check never matched `"Fabric Types:Cotton"`, so every fabric filter was silently dropped despite the field existing. Rather than keep whack-a-moling one missing facet name at a time (this is the third one found this way, after `Neck` and now `Length`/`Fashion Trends`/etc. — Myntra exposes 20+ facets per category and hand-modeling every single one isn't worth it two days before the deadline), added a generic `Constraints.other: dict[str, list[str]]` catch-all field (both `schemas.py` and `types.ts`). The URL parser now extracts *every* `key:value` pair out of the `f=` param generically, pulls out the ones we explicitly model (brand, fabric — accepting both `"Fabric Types"` and `"Fabric"` — sleeve, neck, size, color, occasions), and stashes everything else verbatim in `other` so it round-trips losslessly through save → reload → reapply even though we don't compute drift on it.
**Decisions made where the prompt was ambiguous:** `other` is deliberately NOT in `drift.WEIGHTS` and never will be for arbitrary keys — it exists purely for display/round-trip fidelity, not scoring, since we can't assign a principled weight to a field we don't know the semantics of ahead of time. This is a scope line I'm drawing consciously given the remaining time before submission: named, weighted fields for what the drift engine should reason about; a generic bucket so nothing is ever silently lost even for facets we haven't gotten to.
**Blockers hit / still open:** None — verified against the exact URL from the user's screenshot (`Brand:LULU & SKY,StyleCast::Categories:Dresses::Color:Black_36454f::Fabric Types:Cotton::Length:Mini`) and confirmed `Fabric Types` and `Length` both parse correctly now (`Categories` is intentionally dropped as redundant with the pathname-derived category).

## 2026-07-22 — Root cause of "Save Profile does nothing, zero network requests"
**Master prompt section:** N/A — frontend store bug, not a spec section
**What was implemented:** The user reported clicking "Save Profile" produced no network request at all and the profile just never appeared. Traced it to `useShelfieStore.saveProfile`'s guard clause: `if (!state.liveConstraints || !state.activePersona) return;` — a bare silent `return` with no console output and no user-facing feedback. The screenshot showed the "add persona" input still open with a name typed but not yet confirmed (Enter never pressed), meaning `activePersona` was still `""` (no persona exists yet on a fresh account) when Save was clicked — the guard fired, `fetch` was never called, hence zero network activity and nothing appearing anywhere. This wasn't a backend bug at all.
**Decisions made where the prompt was ambiguous:** Rather than auto-creating a default persona (which would be a bigger, more opinionated UX change), added `console.error` diagnostics to every guard clause in the store (`saveProfile`, `requestSave`, `confirmSave`) so this class of silent no-op is immediately visible in devtools next time, and made `SaveSheet` itself refuse to render a functional-looking save form when there's no active persona — it now shows "Create or select a persona... before saving a profile" instead.
**Blockers hit / still open:** None.

## 2026-07-22 — Part 2 kickoff: baseline regression confirmed before extending
**Master prompt section:** Part 2, Section 0.1
**What was implemented:** Before touching anything, confirmed the existing server was still running (PID still listening on :8000, `/health` OK) and re-ran a full create → drift → commit(new_version) → rollback cycle through curl, including the `neck`/`gender`/`other` fields added in the previous pass. All correct — drift picked up the brand-only change, AI-phrased reason came back, commit produced v2, rollback correctly returned to v1's exact constraints. Baseline confirmed solid; proceeding to Tier 1A per the prompt's own priority order.
**Decisions made where the prompt was ambiguous:** None yet.
**Blockers hit / still open:** None.

## 2026-07-22 — Tier 1A: Collaboration (Publish / Discover / Star / Fork)
**Master prompt section:** Part 2, Section 2
**What was implemented:** Additive migrations (`backend/app/migrations.py`, run after `Base.metadata.create_all()`): `profiles.visibility` (default `'private'`), `profiles.forked_from_profile_id`/`forked_from_version`, and a new `stars` table (composite PK `(account_id, profile_id)` — star count is always `COUNT(*)`, never a maintained integer, so double-clicks/retries can't corrupt it). New endpoints: `PATCH /profiles/{id}/visibility`, `GET /discover` (time-decayed ranking `stars/(age_hours+2)^1.5`, computed at request time, never stored), `POST /discover/{id}/star` (toggle: insert/delete the join row), `POST /discover/{id}/fork` (reads the source's projected state via the existing `project()` function, writes a brand-new independent profile aggregate — never touches the source's own event log). Private profiles return 403 (not a hidden-but-fetchable 200, not a 404) to star/fork attempts from other accounts, and never appear in another account's `/discover` feed at all.
Refactored the profile helpers (`_get_owned_profile`, `_events_for`, `_next_seq`, `_next_version_number`, `_build_history`, `_to_out`) out of `routers/profiles.py` into a new shared `app/profile_ops.py`, specifically so `routers/discover.py`'s fork endpoint reuses the *exact* same fold/projection and version-numbering functions rather than a second implementation — this is a hard requirement from Part 2 Section 0.3, not a style preference, given the fold/version-numbering bugs already found and fixed once.
Added `app/labels.py` for the anonymous `"Shopper-" + account_id[:8]` display label (no username system, per spec) — shared by both `/discover`'s `ownerLabel` and the new `forkedFromOwnerLabel` field on `ProfileVersionOut` (computed live via a join to the source profile's persona/account, not denormalized/stored, consistent with the "compute at query time" philosophy already used for the Discover ranking score).
**Decisions made where the prompt was ambiguous:** For the forked profile's `ProfileCreated` event, used the payload's existing optional `"label"` key (already supported by `projection.apply` for version-history labeling, added in the previous pass) to record `"Forked from {source name} v{source version}"`, instead of appending a redundant second `VersionCommitted` event for the same v1 as a more literal reading of the spec's pseudocode might suggest — this avoids a subtle edge case where rolling a forked profile back to v1 later would need to replay through two events instead of one, and `build_history`'s dedup-by-version logic already produces the exact same single-entry-with-label result either way.
**Blockers hit / still open:** None. Verified end-to-end with two separate `X-Account-Id` values simulating two accounts: private-by-default, 403 on star/fork of a private profile, invisible in the other account's Discover feed pre-publish, then full publish → discover → star → unstar → re-star → fork flow, then — the specific regression this section calls out — committing a `new_version` on the *forked* profile correctly produced v2, not a collision or reused number, confirming the shared version-numbering logic holds for this new code path.

## 2026-07-22 — Tier 1B: Behavioural suggestion toggle
**Master prompt section:** Part 2, Section 3
**What was implemented:** Additive migrations: `accounts.behaviour_tracking_enabled` (default `FALSE`), new `recent_searches` table (`UNIQUE(persona_id, constraints_hash)`, using the same generic SQLAlchemy `JSON` type already used for `events.payload`, not a new inconsistent choice). New endpoints: `GET`/`PATCH /accounts/settings`, `POST /behaviour/observe`. `constraints_hash` is `sha256(json.dumps(constraints, sort_keys=True))[:16]` — canonicalized so the same logical constraint set always hashes identically regardless of field order. The 10-minute cooldown only bumps `last_seen_at` without incrementing `seen_count` if the existing row was touched within the last 10 minutes, so a debounced burst from one real browsing session can't inflate the count. Suppression reuses `drift.compute_drift`/`TAU_LOW` directly against every one of the persona's saved (non-archived) profiles — if any scores below `TAU_LOW` (0.10), the suggestion is suppressed, no second similarity function written.
**Decisions made where the prompt was ambiguous:** None significant — the spec was precise here.
**Blockers hit / still open:** None. Verified the full lifecycle by backdating `recent_searches.last_seen_at` directly in Postgres (can't wait real 10-minute gaps in a single session): seen_count correctly stayed flat within cooldown, incremented once it expired, `suggest` flipped `true` exactly at `seen_count == 3`, and — the specific check the prompt calls out — after saving a profile matching those exact constraints, a subsequent observe call correctly suppressed the suggestion (`suggest: false`) even as `seen_count` kept climbing, confirming the drift-based suppression fires correctly rather than "happening to stop."

## 2026-07-22 — Tier 1A/1B frontend wiring
**Master prompt section:** Part 2, Sections 2.3, 3.3
**What was implemented:** New store state (`discoverFeed`, `behaviourTrackingEnabled`, `actionError`) and new actions (`toggleVisibility`, `fetchDiscoverFeed`, `starProfile`, `forkProfile`, `setBehaviourTracking`) — all additive, no existing action signature touched. Every one of these catches its own failures and sets `actionError` (rendered as a dismissible red banner in `App.tsx`) instead of a bare `console.error`-only silent return, per Section 0.3's explicit requirement that no guard clause anywhere no-ops invisibly again (the "Save Profile does nothing" bug from the previous pass was exactly that class of bug). No existing UI component (`ProfileList`, `SaveSheet`, `Timeline`, `ThreeWaySaveModal`) had its behavior changed beyond additive markup — see `FRONTEND_CHANGES_LOG.md` for the per-file breakdown.
**Decisions made where the prompt was ambiguous:** No "Discover" or "Behaviour" placeholder shell existed anywhere in `extension/src` (checked first, per Section 2.3's instruction) — built both as new components (`panel/Discover.tsx`, `panel/BehaviourPanel.tsx`) styled consistently with the existing card/panel visual language rather than introducing a new pattern.
**Blockers hit / still open:** None. `tsc -b` and `npm run build` both pass clean. Stopping after Tier 1B per the prompt's own guidance ("a solid collaboration layer plus a working core is a stronger submission than a half-built coverage advisor") — Tier 2 (product catalog + coverage/diff) has not been started; awaiting a decision on whether to proceed given the 2026-07-24 1PM IST deadline.

## 2026-07-23 — Tier 2: synthetic product catalog + coverage advisor + dry-run diff
**Master prompt section:** Part 2, Section 4
**What was implemented:** New `products` table (`article_type`/`brand`/`price` each with a btree index, `sizes` with a GIN index for array-overlap queries) via `Base.metadata.create_all()` (a wholly new table, no ALTER needed). `backend/scripts/seed_catalog.py` generates 6,000 explicitly-labeled SYNTHETIC rows across 5 article types reusing vocabulary already seen in real-browser testing (LULU & SKY, StyleCast, Fabindia, Nike, Cotton, and the `Name_hexsuffix` color encoding actually observed from real Myntra URLs — Black_36454f, Grey_808080 — extended consistently for colors not yet observed). Shoe categories (no fabric/sleeve/neck/occasion variation) needed sampling-with-replacement rather than a combo cap, or they'd have had only 10–40 unique rows — noted as a real generation bug caught and fixed before the first successful seed.
New shared `app/catalog_query.py` builds the exact same filter-condition list for both endpoints (never two parallel implementations): `category.articleType` is deliberately excluded from the "relaxable" set coverage suggests removing (relaxing the base category isn't a meaningful suggestion the way relaxing brand/color is). `POST /coverage` counts current matches, then for each populated relaxable field, the count with just that field removed, returning the top 2–3 by gain. `POST /diff` computes added/removed as AND-NOT boolean pairs (equivalent to `EXCEPT` on this single table, per the prompt's "EXCEPT or equivalent") — never diffs two ID lists in Python — plus the modal (most frequent) brand among the added set via `GROUP BY`.
**Decisions made where the prompt was ambiguous:** None major — the spec was precise on query shape and the "don't load the whole table into Python" constraint.
**Blockers hit / still open:** None. Verified with `EXPLAIN`: the combined article_type+brand+price query used Bitmap Index Scans on all three relevant indexes (no sequential scan), and the size-overlap query used `idx_products_sizes_gin` — confirmed via `docker exec ... psql ... EXPLAIN`, not just assumed. Coverage/diff endpoints tested against real generated data: narrowing all filters correctly drove `currentCount` to 0 with color relaxation showing the biggest gain; adding a second brand to `/diff` correctly reported the right added-count and correctly identified the new brand as `addedSampleBrand`; narrowing price correctly showed only removals.

## 2026-07-23 — Tier 3: global negative rules + fuzzy NL→filters compiler
**Master prompt section:** Part 2, Section 5
**What was implemented:** Additive `personas.global_exclusions` JSON column (brand/fabric/color lists), new `GET`/`PATCH /personas/{id}/exclusions` endpoints, applied as always-present `NOT IN` conditions inside `catalog_query.build_conditions` whenever a `personaId` query param is supplied to `/coverage` or `/diff` — verified a persona excluding "Polyester" saw its coverage count drop from 1200 to 796 on the same query, while the same request without `personaId` was unaffected. Did not attempt to wire exclusions into `drift.py`'s comparison itself — the spec's own connection between "global exclusions" and "drift comparisons" is thin/ambiguous (drift compares two constraint sets' field-level differences, it doesn't evaluate catalog matches), and this whole tier is explicitly flagged in the prompt as "lowest priority... only build if genuinely nothing else is left," so I scoped it to the concrete, well-specified catalog-filtering use.
NL→filters compiler: new `app/ai/compile_intent.py` with a hand-maintained lexicon (reusing the same vocabulary as `seed_catalog.py` and the rest of the app) mapping fuzzy language to concrete constraint values, plus a numeric-only special case for `price_min`/`price_max` (not lexicon-mappable, validated as a non-negative number instead). New `POST /ai/compile-intent` endpoint: Groq (via the existing key-rotation client, no second client built) proposes `{attribute, value}` pairs; `validate_and_merge` drops anything not found in the lexicon before it's ever included in the response — strict propose-then-validate, never trust-and-apply.
**Decisions made where the prompt was ambiguous:** None major for the compiler itself.
**Blockers hit / still open:** None. Verified with a realistic sentence ("pure cotton kurtas under 1500 for school, nothing flashy") — correctly extracted category/fabric/occasion/price.max and correctly ignored "nothing flashy" (no lexicon entry). Verified with a deliberately nonsense sentence (invented brand "Zorbex", "quantum-inspired") — validator correctly returned an empty patch, proving the validator actually rejects rather than passing through whatever the LLM proposes.
**Final regression check across all tiers:** Restarted the server clean (no `--reload`, per the standing gotcha), re-ran the full personas → profile → drift → commit → rollback cycle once more after all catalog/exclusion/compiler code was in place — unaffected. Confirmed via `/openapi.json` that all routes from every tier built this session (profiles, discover, behaviour, catalog, ai) are registered with no path collisions.
